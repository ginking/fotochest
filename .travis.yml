language: python
python:
  - "2.6"
  - "2.7"
# command to install dependencies
install: pip install -r conf/requirements.txt
# command to run tests
script: manage.py test photo_manager --settings=settings.local
